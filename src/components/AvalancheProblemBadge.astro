---
interface Props {
  name: string;
}

const { name } = Astro.props;

const iconByProblem: Record<string, { symbol: string; tone: string }> = {
  "Wind Slab": { symbol: "🌬", tone: "wind" },
  "Storm Slab": { symbol: "🌩", tone: "storm" },
  "Wet Snow": { symbol: "💧", tone: "wet" }
};

const icon = iconByProblem[name];
const tone = icon?.tone ?? "standard";
const symbol = icon?.symbol ?? "⚠";
---

<span class="problem-badge" title={name} aria-label={name}>
  <span class:list={["problem-icon", `problem-icon--${tone}`]} aria-hidden="true">{symbol}</span>
  <span>{name}</span>
</span>
